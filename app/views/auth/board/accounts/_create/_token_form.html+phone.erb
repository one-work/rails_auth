<%= form_with theme: 'phone_modal', model: @account, scope: '', url: { action: 'confirm', id: @account.id }, method: :post do |f| %>
  <%= render 'modal_header' %>
  <section id="modal_card_body" class="modal-card-body">
    <%= f.text_field :token %>
  </section>
  <%= f.submit %>
<% end %>
